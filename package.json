{
  "name": "kippu-api",
  "version": "0.0.0",
  "private": true,
  "description": "Kippu platform APIs",
  "license": "UNLICENSED",
  "type": "module",
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && pnpm --filter \"./packages/**\" build",
    "typecheck": "tsc --noEmit && pnpm --filter \"./packages/**\" typecheck",
    "lint": "biome ci .",
    "format": "biome check --write .",
    "test": "vitest run",
    "lint:personal-data": "pnpm --filter @kippu/metadata-schema lint:personal-data",
    "check:contract": "scripts/check-api-contract.sh",
    "changeset": "changeset",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@trpc/server": "11.18.0",
    "fastify": "5.12.4"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.13",
    "@changesets/cli": "3.0.2",
    "@trpc/client": "11.18.0",
    "@types/node": "24.13.4",
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
