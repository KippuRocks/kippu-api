---
"@kippurocks/sponsorship": minor
---

First version: the sponsorship codec bound to an operation id or pass id, its verification, and the sponsor's `p256` KMS signer interface with a software stand-in under `/testing` (`T-023-01`).
