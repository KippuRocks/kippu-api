---
"@kippurocks/api": minor
---

A holder's own credentials (`T-025-13`). `derived.credentials.mine`, for a linked holder session only, lists the credentials registered to the holder's account, in registration order: each credential id, when the ledger recorded its registration, and whether it is the credential this session was linked with. No other principal can read it.
